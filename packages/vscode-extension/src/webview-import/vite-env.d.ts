/// <reference types="vite/client" />

// Brings in Vite's module declarations for suffixed imports — notably
// `?worker`, which the built-in import engine uses to spawn the conversion
// worker (see VsCodeImportApp.tsx).
